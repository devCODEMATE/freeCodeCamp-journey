# 🔄 String Transformer

## 🇪🇸 Español

Este es el séptimo ejercicio del módulo de JavaScript en freeCodeCamp.

El objetivo fue practicar métodos de strings para transformar texto, reemplazar palabras y repetir contenido.

### Conceptos practicados
- Strings
- `.replace()`
- `.replaceAll()`
- `.repeat()`
- `.trimEnd()`
- Template literals
- `console.log()`

---

## 🇺🇸 English

This is the seventh exercise from the JavaScript module in freeCodeCamp.

The goal was to practice string methods to transform text, replace words, and repeat content.

### Concepts practiced
- Strings
- `.replace()`
- `.replaceAll()`
- `.repeat()`
- `.trimEnd()`
- Template literals
- `console.log()`

---

## 📁 Archivo | File

- `script.js`
